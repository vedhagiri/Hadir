# Hadir Grafana dashboard provisioning (v1.0 P26).
# Loads every JSON file under ``/var/lib/grafana/dashboards/``
# at boot. The Hadir dashboard itself lives in
# ``deploy/grafana/dashboards/hadir.json`` (mounted into the
# Grafana container by docker-compose.prod.yml).

apiVersion: 1

providers:
  - name: hadir
    orgId: 1
    folder: Hadir
    type: file
    disableDeletion: false
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
