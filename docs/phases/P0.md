# P0 — Branching + v1.0 bootstrap

**Phase:** P0 (v1.0 plan, milestone "—")
**Date (UTC):** 2026-04-25
**Engineer:** Suresh (MTS) via Claude Code
**Plan reference:** `v1.0-phase-plan.md §"P0 — Branching + v1.0 bootstrap"`

## Goal

Pilot frozen at tag `v0.1-pilot`. `release/pilot` branch ready for
hotfixes. `main` cleaned up and ready for v1.0 work. `CLAUDE.md`
flipped to v1.0 mode.

## What was done

- Created annotated tag `v0.1-pilot` pointing at commit `1a0782c`
  (tip of `main` at the time, the last commit of the pilot stream:
  `docs(P14): pre-flight artifacts for Omran deployment`).
- Created branch `release/pilot` from the tag. Both pushed to origin
  (`https://github.com/vedhagiri/Hadir.git`).
- Confirmed working branch: `main`.
- Updated `CLAUDE.md`:
  - Top banner: "v1.0 in development. Pilot frozen at tag
    `v0.1-pilot` on branch `release/pilot`."
  - New "Branching" section explaining the model
    (`release/pilot` for hotfixes; `main` for v1.0; milestone tags
    `v1.0-m2` / `v1.0-m3` / `v1.0` per the v1.0 phase plan).
  - Reading-order updated to point new sessions at
    `v1.0-phase-plan.md` (active) and `pilot-plan.md` (historical).
  - Status flipped to "v1.0 phase currently complete: P0".
  - Pilot's "What P# built" content preserved verbatim under a new
    "Pilot build log (historical, for context)" section.
- Marked `pilot-plan.md` historical with a one-line banner pointing
  at `v1.0-phase-plan.md`.
- Created `docs/phases/` and this file.

## Tag + branch verification

```
$ git tag -l
v0.1-pilot

$ git rev-parse "v0.1-pilot^{commit}"
1a0782c17fe55f1df6fd3ba0ad72b7509026a81c

$ git cat-file -p v0.1-pilot
object 1a0782c17fe55f1df6fd3ba0ad72b7509026a81c
type commit
tag v0.1-pilot
tagger Vedhagiri <…>
Pilot delivered to Omran, per BRD §15.1 signoff

$ git branch -a
* main
  release/pilot
  remotes/origin/main
  remotes/origin/release/pilot

$ git push origin v0.1-pilot release/pilot
* [new tag]    v0.1-pilot -> v0.1-pilot
* [new branch] release/pilot -> release/pilot
```

## Notes for whoever resumes next

- `docs/pilot-signoff.md` at the tagged commit still reads
  `STATUS: not yet delivered` — the actual signed checklist is held
  out-of-band by MTS, not in the repo. The tag annotation
  ("Pilot delivered to Omran, per BRD §15.1 signoff") is the
  authoritative record going forward; the in-repo template was kept
  unfilled by intent (no customer signature in public-ish git
  history). If a future audit needs the in-repo file to match, fill
  in `docs/pilot-signoff.md` on `release/pilot`, commit as a
  hotfix-style entry, but do **not** retag — `v0.1-pilot` stays at
  `1a0782c`.
- The next session is **P1 — Multi-tenant routing switch (M2)**.
  Don't start it without the user's go-ahead.

## Convention going forward

Every phase from now on commits one `docs/phases/P<N>.md` alongside
its code in the same git commit. Phase records carry: date,
goal restated, what was done (bullet list), verification snippets,
notes for the next session. Keep them short — the build log and
commit messages already carry the weight; this file is the
"what changed and why" header.
